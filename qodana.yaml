version: "2024.1"

profile:
  name: qodana.recommended

include:
  - name: CheckDependencyLicenses
  - name: PySecurityInspection

exclude:
  - name: All
    paths:
      - .venv
      - tests
      - artifacts
      - .idea
