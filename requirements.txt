# Runtime dependencies — mirrors [project.dependencies] in pyproject.toml.
# Kept for PyCharm "Unsatisfied package requirements" inspection and
# for environments that don't support PEP 621 pyproject.toml.
fastapi>=0.109.0
uvicorn>=0.27.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
sqlalchemy>=2.0.0
apache-airflow>=2.8.0
dbt-core>=1.7.0
scikit-learn>=1.4.0
pandas>=2.2.0
numpy>=1.26.0
chromadb>=0.4.22
sentence-transformers>=2.5.0
python-jose>=3.3.0
cryptography>=42.0.0
passlib>=1.7.4
# Pinned <4.1.0: passlib 1.7.4 calls bcrypt C-binding internals that changed in
# bcrypt 4.1.0.  Upgrading bcrypt without first migrating off passlib will
# break ALL password hashing/verification.  Lift this pin only after passlib
# is replaced with direct bcrypt (or argon2-cffi) usage.
bcrypt>=4.0.0,<4.1.0
python-multipart>=0.0.6
httpx>=0.26.0
joblib>=1.3.0
# Pin pathspec to the highest version that satisfies both apache-airflow (>=0.9)
# and dbt-core (<0.13).  black/mypy require >=1.0 which is irreconcilable with
# dbt-core's upper bound; both tools still function correctly with 0.12.x.
pathspec==0.12.1
